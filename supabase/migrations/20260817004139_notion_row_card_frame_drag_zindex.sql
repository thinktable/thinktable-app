-- Frontend-only marker (no schema change).
-- Notion row→card convert; frame drag uses zIndex (not nodes reorder) so TipTap
-- NodeViews stay mounted; row-card layout freeze; DB hug uses table scrollWidth.
-- Schema unchanged.
select 1;
