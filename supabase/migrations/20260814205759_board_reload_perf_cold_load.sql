-- Frontend-only marker (no schema change).
-- Board cold-load perf: drop 500ms messages poll; homepage probe only for homepage id;
-- async legacy migrate; loading.tsx; RF Node type-import fix (no endless Fast Refresh).
-- Schema unchanged.
select 1;
