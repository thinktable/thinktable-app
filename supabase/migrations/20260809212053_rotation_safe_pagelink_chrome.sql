-- Frontend-only marker (no schema change).
-- Rotation-safe pageLink / ⋮⋮ chrome: lib/dom-transform (elementUniformScale +
-- local↔screen), glyph-line Range placement, AABB-anchored upright frame chrome
-- when rotated; fit-to-text lock icon; brand mark AI star accent.
-- All via client UI — no DDL.
select 1;
