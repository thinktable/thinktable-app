-- Frontend-only marker (no schema change).
-- Board pan/zoom nav freeze; Notion DB subtasks + column widths from view config;
-- cell overflow ellipsis; phone DB touch / paint hygiene.
-- Schema unchanged.
select 1;
