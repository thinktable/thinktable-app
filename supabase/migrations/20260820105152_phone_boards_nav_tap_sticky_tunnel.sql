-- Frontend-only marker (no schema change).
-- Phone boards nav: scrim below top bar, tap opens board + closes, hold reorders;
-- sticky Cloudflare tunnel script replaces prior tunnel on each run.
-- Schema unchanged.
select 1;
