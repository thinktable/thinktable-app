-- Frontend-only marker (no schema change).
-- Remove Style bar from mode pill; frame/thread menu no search autofocus on touch;
-- second strip/path tap toggles menu closed.
-- Schema unchanged.
select 1;
