-- Frontend-only marker (no schema change).
-- Block/frame menus: Turn into Format / Property tabs; Block vs Frame
-- headers; hide Turn into + Present from here on the appropriate menu.
-- Schema unchanged.
select 1;
