-- Frontend-only marker (no schema change).
-- Phone mode pill: tools in the left dropdown pill; even Draw icon gaps; undo/redo
-- sibling with board fill tucked under the tools right cap.
-- Schema unchanged.
select 1;
