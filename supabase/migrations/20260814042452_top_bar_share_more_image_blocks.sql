-- Frontend-only marker (no schema change).
-- Top bar: lock+Share, copy link, favorites, More (Import/Export, Connections→Notion).
-- Layout Table moved right; Draw eraser sits with pencil/highlighter.
-- TipTap imageBlock (Turn into → Image). Favorites use conversations.metadata.favorite.
-- Schema unchanged.
select 1;
