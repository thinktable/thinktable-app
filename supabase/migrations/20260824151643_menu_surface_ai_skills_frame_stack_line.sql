-- Frontend-only marker (no schema change).
-- Menu surfaces: `.tt-menu-surface` translucent blur on frame/block/board/thread menus + flyouts.
-- Menu placement: side-slot beside host frame; visual adjust-box measure; flyout lock scoring.
-- AI: suggest-edits skill; attach-skill helper; seedSkillIds on composer; chat sidebar wiring.
-- Frame stack line: gap between simulated connection points (`lib/frame-stack-line.ts`).
-- Schema unchanged.
select 1;
