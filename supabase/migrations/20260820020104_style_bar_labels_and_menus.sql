-- Frontend-only marker (no schema change).
-- Style bar: Block / Frame / Thread menus + labels (no “style” in tool names);
-- Layout bar: Thread layout → Threads; Turn into / Notion pin prefs polish.
-- Schema unchanged.
select 1;
