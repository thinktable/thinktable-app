-- Frontend-only marker (no schema change).
-- Top bar + Free nav: | pipes → /; Draw group dividers → /; keep / before More
-- and More→Layout when the Actions bar overflows. Schema unchanged.
select 1;
