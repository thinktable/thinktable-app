-- Frontend-only marker (no schema change).
-- Notion DB editing + Thinktable view settings: editable cells (PATCH page
-- properties), multi-select/schema option menus, API 2025-09-03 data_sources,
-- viewSettings on databaseBlock (property order/hide, filter, sort, group,
-- conditional color, sub-tasks, layouts). Schema unchanged.
select 1;
