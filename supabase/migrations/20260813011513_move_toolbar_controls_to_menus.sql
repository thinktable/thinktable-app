-- Frontend-only marker (no schema change).
-- Move Home top-bar formatting off the bar: paintbrush + align to text select
-- popup; fill/border + board/frame locks to frame right-click menu; thread
-- Style/Thickness (strokeWidth 1–4px) to thread click menu. Schema unchanged.
select 1;
