-- Frontend-only marker (no schema change).
-- AI composer chrome: Ask↔Edit toggle in input row; + menu with search,
-- skills (Summarize / Tasks / Search page / Quiz me), File, Connection;
-- menu portaled above overflow-hidden shell.
-- Schema unchanged.
select 1;
