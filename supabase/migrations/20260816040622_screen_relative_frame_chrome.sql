-- Frontend-only marker (no schema change).
-- Frame selection chrome is screen-relative (threadComfortScale / zoom×frameScale comfort):
-- resize handles, connection indicators, rotate/free/wrap, property/connections rows, ⋮⋮ grips.
-- Schema unchanged.
select 1;
