-- Frontend-only marker (no schema change).
-- Chat turn multi-select (Shift/Cmd/Ctrl) + chat↔chat threads (targetTurnId);
-- multi-selected turns drag together; thread connect auto-pan sign fix.
select 1;
