-- Frontend-only marker (no schema change).
-- Notion DB table perf: paginated load (50 rows + Load more), @tanstack/react-virtual row
-- virtualization, lazy cell editors, memoized rows, frame-drag light shell.
-- DB wheel: scroll in Scroll nav; block pan at edges; pinch/Cmd+wheel zoom still reaches map.
-- Bottom fade on truncated table; internal scroll when frame selected.
-- Schema unchanged.
select 1;
