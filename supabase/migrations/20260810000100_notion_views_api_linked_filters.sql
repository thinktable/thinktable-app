-- Frontend-only marker (no schema change).
-- Notion Views API for linked DB embeds: list/retrieve views, POST
-- /v1/views/{id}/queries (filter/sorts/quick_filters applied server-side),
-- hydrate rows from data_sources; linked embeds no longer dump the full source.
-- Also: table chrome (no outer perimeter), row/block add hairlines, ⋮⋮ grip.
-- Schema unchanged.
select 1;
