-- Frontend-only marker (no schema change).
-- On-thread offset frames: drag commit persists perpendicular snap; flip to opposite side when crossing the thread;
-- live drag anchor during projection; skip block-group drag-stop for on-thread frames.
-- All state rides existing messages.metadata JSON. Schema unchanged.
select 1;
