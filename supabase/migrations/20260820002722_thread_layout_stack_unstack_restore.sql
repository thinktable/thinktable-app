-- Frontend-only marker (no schema change).
-- Thread layout: magnet/stack toggles independent of align/direction; stack collapses
-- in place; unstack unhides in place; selection-chrome unwind on hide remount.
-- Schema unchanged.
select 1;
