-- Frontend-only marker (no schema change).
-- Property strip: horizontal scroll + … overflow hint (replaces caret pagination).
-- Free-resize DB frames: hover clip-preview shows full table (all columns × rows).
-- TipTap content-sync guard when editor view is destroyed.
-- Schema unchanged.
select 1;
