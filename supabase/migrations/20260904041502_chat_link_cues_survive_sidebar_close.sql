-- Frontend-only marker (no schema change).
-- Board chat-link cues sync from ChatSidebar messages so simulators stay
-- chat-connected while desktop chat is closed; thread clip under chrome.
select 1;
