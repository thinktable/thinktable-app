-- Frontend-only marker (no schema change).
-- Frame menu Table rows: row setter (editable shown/total, Show all, Reset).
-- Replaces Preview / Expanded; still persists metadata.dbVisibleRowCap.
select 1;
