-- Frontend-only marker (no schema change).
-- Phone: preview / open / Notion on boardLink frames work — selected-frame
-- caret touchstart skips [data-page-link-preview]; menu uses nodrag/nopan;
-- touch devices keep the pill visible while the host frame is selected.
-- Schema unchanged.
select 1;
