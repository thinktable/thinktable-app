-- Frontend-only marker (no schema change).
-- AI Edit: hide text via [[hide]]…[[/hide]] / haze spans (flashcards Q/A toggle); context pack exports hidden spans.
-- Selected frames: TipTap pointerdown/mousedown stopPropagation only — native drag-select works again.
-- All state rides existing messages.content HTML + ai_threads metadata. Schema unchanged.
select 1;
