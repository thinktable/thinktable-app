-- Frontend-only marker (no schema change).
-- Frame edge-snap + stack line (Eye/Open stack, directional Stack arrows, Lock);
-- frames-as-shapes; upright blue AABB resize chrome with rotated content;
-- nested TipTap frames removed (unwrap on load). Schema unchanged.
select 1;
