-- Frontend-only marker (no schema change).
-- Property header as one block (single ⋮⋮); propertyBlock atom cells; chat brand
-- beside thread picker; path/tool More cutoff; boards-nav pin persist.
-- Schema unchanged.
select 1;
