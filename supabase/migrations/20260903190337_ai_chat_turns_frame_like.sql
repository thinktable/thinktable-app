-- Frontend-only marker (no schema change).
-- AI chat turns are frame-like: select → blue adjust + edge connection points;
-- TipTap + ⋮⋮ (arm-on-press, shift multi-select); drag turn/blocks onto board as frames;
-- boardLinks threads show on select (pip when not). Soft-save PATCH { soft: true }.
select 1;
