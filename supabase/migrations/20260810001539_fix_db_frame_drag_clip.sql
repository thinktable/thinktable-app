-- Frontend-only marker (no schema change).
-- Fix Notion databaseBlock frames “disappearing” on drag: z-order bump once
-- at drag start; pause hug measure while dragging; stable TipTap editor options
-- so NodeViews don’t remount; reject/heal collapsed ~52×40 resizeDimensions
-- persisted from stub measures before `.tt-notion-db` mounts.
-- Schema unchanged.
select 1;
