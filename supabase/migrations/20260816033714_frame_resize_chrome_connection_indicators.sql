-- Frontend-only marker (no schema change).
-- Frame resize chrome: 1:1 linear frameUiScale (compact handles), square adjust ring;
-- connection simulators sit further out; indicator press no longer frame-drags.
-- Schema unchanged.
select 1;
