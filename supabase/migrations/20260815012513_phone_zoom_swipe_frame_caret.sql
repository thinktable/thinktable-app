-- Frontend-only marker (no schema change).
-- Phone Zoom nav: two-finger swipe-zoom + coast (no pan); Scroll nav pan+coast.
-- Selected-frame caret: non-passive phone touchstart + desktop pointerdown.
-- Schema unchanged.
select 1;
