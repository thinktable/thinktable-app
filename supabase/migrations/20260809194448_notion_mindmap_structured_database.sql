-- Frontend-only marker (no schema change).
-- Notion Generate mindmap: child_page + child_database tree, parent→child threads,
-- pages as title pageLink + Pages menu nesting; databases as in-frame structured
-- table via /api/notion/database/[id] (live Notion properties/rows).
-- All via app + Notion API — no DDL.
select 1;
