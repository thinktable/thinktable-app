-- Frontend-only marker (no schema change).
-- Desktop: selecting a frame opens the frame menu (like thread/block);
-- phone tap-after-selected drag-strip behavior unchanged.
-- Schema unchanged.
select 1;
