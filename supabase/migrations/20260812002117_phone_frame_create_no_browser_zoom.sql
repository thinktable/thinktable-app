-- Frontend-only marker (no schema change).
-- Stop browser page-zoom / focus-scroll when creating frames on phone:
-- viewport maximumScale=1; board main overflow-hidden; I-bar capture stays
-- centered at 16px and keeps keyboard focus (TipTap setContent only);
-- create path no longer forces RF zoom to 100%. Schema unchanged.
select 1;
