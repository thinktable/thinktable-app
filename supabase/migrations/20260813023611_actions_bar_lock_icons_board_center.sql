-- Frontend-only marker (no schema change).
-- Actions bar: board lock = Anchor, frame lock = stacked Lego brick; Free nav
-- Scroll|Zoom pipes; board-centered toolbar (no top-bar +, no mode-pill reset);
-- Insert drops Link/Symbols/Date; layout (None) Actions-only. Schema unchanged.
select 1;
