-- Frontend-only marker (no schema change).
-- DB row warm: inline single-row editors + caret at click; switch rows without nav.
-- Removed semantic zoom (<40% simplify). Keep TipTap live through nav for already-live /
-- DB frames so tables paint mid-gesture. I-bar retained after cold→warm focus handoff.
select 1;
