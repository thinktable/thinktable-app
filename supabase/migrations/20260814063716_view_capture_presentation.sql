-- Frontend-only marker (no schema change).
-- View bar: Capture + Presentation menus (local captures, presentation picker, add-to-chat).
-- Draw highlighter color dropdown; board menu Capture; connections group on frames.
-- Schema unchanged.
select 1;
