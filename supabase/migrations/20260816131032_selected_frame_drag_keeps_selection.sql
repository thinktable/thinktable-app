-- Frontend-only marker (no schema change).
-- Selected frame drag keeps selection (body/chrome — not text, ⋮⋮, adjust,
-- connection simulators, or property/connection marks); mid-press hides
-- indicators only. Phone: all block ⋮⋮ while frame selected; menu placement.
-- Schema unchanged.
select 1;
