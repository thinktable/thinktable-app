-- Frontend-only marker (no schema change).
-- Connections strip gets a ⋮⋮ like the property header (Live Sync / Manual / Remove);
-- property + connections chrome grips omit add-block hairlines.
-- Schema unchanged.
select 1;
