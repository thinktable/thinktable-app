-- Frontend-only save: page open menu on page frames + clamp inside unlocked narrow frames.
-- No schema changes. Page/frame link state stays in messages.content (pageLink) + messages.metadata.
SELECT 1;
