-- Frontend-only marker (no schema change).
-- AI Edit: create frames + threads (pending review), markdown→TipTap task lists,
-- prefer edits over duplicate creates, capabilityGap confirm before approximating
-- unsupported asks (e.g. real tables), checklist CSS/handles, hide top-bar AI
-- origin toggle when page has no AI-origin content.
-- Relies on existing ai_* tables from 20260810020000_ai_copilot_foundation.
-- Schema unchanged.
select 1;
