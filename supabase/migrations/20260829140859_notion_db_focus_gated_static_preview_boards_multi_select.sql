-- Frontend-only marker (no schema change).
-- Notion DB frames: focus-gated live table, always-compact static preview, nav-freeze watchdog.
-- Boards nav multi-select (Shift / ⌘ click) with bulk share / move / delete + block↔page sync.
-- Schema unchanged.
select 1;
