-- Frontend-only marker (no schema change).
-- Frame fit↔free toggle: unlock restores saved free-resize box (unlockedFrameSize);
-- lock snapshots live free dims before hugging; metadata sync no longer clobbers restore.
-- Schema unchanged.
select 1;
