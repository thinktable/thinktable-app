-- Frontend-only marker (no schema change).
-- DB row footer: `+# rows — show more / show less` (paged ±50; compact floor 12).
-- Idle table preview hugs full width / all view-visible columns (no 720px / 8-col caps).
select 1;
