-- Frontend-only marker (no schema change).
-- Chat sidebar reload persistence (open + active thread via localStorage);
-- GET /api/ai/threads/[id] for restore; live context pills (page/frame/block/text)
-- with hover previews; skills attach as pills (not pasted prompts).
-- Schema unchanged.
select 1;
