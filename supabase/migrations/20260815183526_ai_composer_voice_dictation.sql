-- Frontend-only marker (no schema change).
-- AI composer voice: mic left of send; Cursor-style waveform strip;
-- PCM→WAV → POST /api/ai/transcribe (Whisper); skip virtual mics (BlackHole).
-- Schema unchanged.
select 1;
