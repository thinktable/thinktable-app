-- Frontend-only marker (no schema change).
-- Shift+drag flips Free-nav pointer tool (pan↔select).
-- Cmd/Ctrl+wheel flips sticky Scroll↔Zoom (Mac pinch always zooms).
-- New board default title + in-path rename; empty /board shows New board.
-- Schema unchanged.
select 1;
