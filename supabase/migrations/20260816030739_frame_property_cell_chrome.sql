-- Frontend-only marker (no schema change).
-- Frame property cell chrome: fill radius matches cell; ⋮⋮ centers in adjust gutter and
-- tracks frameScale; property/connections only while selected; empty cell first-click
-- selects the frame (no caret); hover border only when selected; editable sync so
-- selected hover/I-bar work on first interaction.
-- Schema unchanged.
select 1;
