-- Frontend-only marker (no schema change).
-- Toolbar overflow → mode pill (shareCompact then phoneTools); phone chat forces pill;
-- chat sidebar Notion seam resize + preferred width across shrink/expand;
-- chat frames full-width with side pad; filter/sort left-align; phone layout 768.
select 1;
