-- Frontend-only save: locked frame fit + Notion database blocks.
-- Locked frame hug — symmetric width (gutter read from the ⋮⋮ handles row, right margin
--   mirrors the ⋮⋮ icon's left inset) and hug HEIGHT to content on lock/relock/type.
-- pageLink open-menu spill clamp scoped to the panel (was matching the React Flow pane on
--   locked frames, so the menu escaped the right edge).
-- Notion database blocks: databaseBlock TipTap atom + view, migrate-frame, blocks-to-html /
--   import-to-board updates, Notion mark/color assets, connect/import chrome.
-- No schema changes.
SELECT 1;
