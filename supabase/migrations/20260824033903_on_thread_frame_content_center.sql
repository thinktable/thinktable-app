-- Frontend-only marker (no schema change).
-- On-thread frames: compact chip layout (no empty bottom chrome band); content vertically centered;
-- path projection uses measured RF node box (onThreadFrameVisualSize); restore isBoardBodyMeta import.
-- All state rides existing messages.metadata JSON. Schema unchanged.
select 1;
