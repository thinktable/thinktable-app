-- Frontend-only marker (no schema change).
-- Unify AI sparkle to light blue #b5daf3 on every logo that shows stars;
-- toolbar lock-frames glyph uses group frames icon 1 (readable at h-4).
select 1;
