-- Frontend-only marker (no schema change).
-- Thread click menu (ThreadActionsMenu): Notion-style chrome like BlockActionsMenu;
-- wired Delete / Insert frame / Collapse / Dotted / Arrange path styles / Copy-Paste style.
-- Sidebar: click-pin Pages nav (isSidebarPinned) survives leave + page switch.
-- All via client UI — no DDL.
select 1;
