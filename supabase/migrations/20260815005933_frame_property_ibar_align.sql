-- Frontend-only marker (no schema change).
-- Turn into → Property stamps metadata.propertyType; frame top property icon +
-- connection handle; I-bar spawn offsets match block chrome (X=26, Y=4).
-- Schema unchanged.
select 1;
