-- Frontend-only marker (no schema change).
-- Customize-agent panel; chat return-to-bottom (phone + desktop); open chats
-- pin to bottom; preserve scroll across phone↔desktop remounts; thread caret
-- beside title; header agent icon only when transcript exists.
-- Schema unchanged.
select 1;
