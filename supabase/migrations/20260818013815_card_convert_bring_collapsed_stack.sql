-- Frontend-only marker (no schema change).
-- Nested/parent Card convert bring-along (sub + parent, both default on);
-- collapsed sideStacks stack; peeled rows leave the live table; DB gutter/pinch polish.
-- Schema unchanged.
select 1;
