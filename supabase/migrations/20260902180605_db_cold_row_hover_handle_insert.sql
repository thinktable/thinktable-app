-- Frontend-only marker (no schema change).
-- Cold Notion DB rows: ⋮⋮ handle + add-row hairlines on hover without click-warm.
-- One gutter mount per hovered row; create-row POSTs into shared react-query cache.
select 1;
