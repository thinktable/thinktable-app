-- Frontend-only marker (no schema change).
-- Top property strip: overflow-clipped pagination with left/right carets; measures chrome band width.
-- Property icons (top strip + in-frame cells): shared delayed name popup (Notion column name, else type label).
-- Card↔table property round-trip: propertyName on propertyBlock; inline header harvest.
-- Schema unchanged.
select 1;
