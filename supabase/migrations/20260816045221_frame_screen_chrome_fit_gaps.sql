-- Frontend-only marker (no schema change).
-- Frame screen chrome: boosted frameScreenChromeScale; blue→content gaps (X/Y);
-- rotate/free/wrap gap matches ⋮⋮ gutter; add-block hairlines even from visual ⋮⋮.
-- Schema unchanged.
select 1;
