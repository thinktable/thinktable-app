-- Frontend-only marker (no schema change).
-- Menu flyouts always open to the right of the parent card;
-- lock main card under pointer when a flyout is open (no hover thrash).
-- Schema unchanged.
select 1;
